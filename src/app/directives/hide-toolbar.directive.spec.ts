import { HideToolbarDirective } from './hide-toolbar.directive';

describe('HideToolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new HideToolbarDirective();
    expect(directive).toBeTruthy();
  });
});
